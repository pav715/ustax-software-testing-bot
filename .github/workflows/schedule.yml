name: Hourly Schedule

# DISABLED — cron-job.org triggers Hourly Orchestrator on ustax-jobs-bot instead.
# Kept for manual testing only (workflow_dispatch).
on:
  workflow_dispatch:

permissions:
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Manual dispatch info
        run: |
          echo "Auto schedule is handled by cron-job.org -> Hourly Orchestrator."
          echo "To run the bot now, use Bot Auto Run workflow."
